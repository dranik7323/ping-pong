from pygame import *

okno = display.set_mode((700,500))
display.set_caption('чота')
#задай фон сцены
okno.fill(52, 157, 207)

game = True



clock = time.Clock()
fps = 60

while game:
    for e in event.get():
        if e.type == QUIT:
            game = False
clock.tick(fps)
display.update()

